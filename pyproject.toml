[tool.poetry]
name = "todolist-oop-v2"
version = "0.1.0"
description = "A simple TodoList application"
authors = ["zahraaa <zahraaa@example.com>"]
readme = "README.md"
packages = [{include = "todolist_oop"}]

[tool.poetry.dependencies]
python = "^3.10" 
python-dotenv = "^1.1.1"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"